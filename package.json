{
  "name": "wcloud-agent",
  "version": "0.1.0",
  "description": "Per-server agent for wcloud. Exposes deploy/update/delete/ssl/purge over an authenticated HTTP API with live SSE logs.",
  "type": "module",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js"
  },
  "engines": {
    "node": ">=18"
  },
  "dependencies": {
    "express": "^4.19.2"
  }
}